# HANDOFF — модел за прогноза на мъгла, BULATSA
## Състояние към 31.07.2026, край на сесията

---

## 1. КАКВО Е ЗАПЕЧАТАНО СЕГА

**Активни репери:** `LBBG-v9`, `LBGO-v9`, `LBPD-v8`, `LBSF-v12`, `LBWN-v11`

```
HIT=37  MISS=30  FA=36  CN=185   CSI=0.359   MAE_T=1.658 °C   288 случая
```

Кодът съдържа три поправки от тази сесия, всичките в `fog_model.py`:
1. Географска дължина в слънчевия часов ъгъл
2. Уравнение на времето (Spencer 1971)
3. Изгревен обмен: рампа на `C_H` + растящ смесен слой `DZ`

Плюс `run_case.py` носи флага за надеждност в изхода, а `verify_cases.py` —
поправен гейт (падналите случаи вече не се броят за успех).

**Незавършено:** `features.csv` и `reliability.json` са от **преди**
изгревната поправка. Трябва да се преизчислят:
```
python collect_features.py --restart
python calibrate_reliability.py
```

---

## 2. ГЛАВНОТО ОТКРИТИЕ НА СЕСИЯТА

Илиан забеляза от оперативен опит: „при ранен старт 16 UTC нагряването
продължава до 20–21, сутрин охлаждането до 09 — сякаш изгревът и залезът
са в локално време."

Диагнозата беше един ред:
```python
ha = np.deg2rad((hour_utc - 12.0) * 15.0)   # слънчево пладне в 12:00 UTC
```
Пладнето е в 12:00 UTC само на Гринуич. За България (23.4–27.8° и.д.) е в
10:09–10:26 → **моделното слънце изоставаше със 110 минути.**

Измерено преди поправката: залез лято 19:35 вместо 17:45; изгрев зима
07:35 вместо 05:50. Зимата личи по-слабо, защото при старт 18 UTC и двата
залеза са преди началото.

**Резултат на 288 случая:** HIT 41→38, FA 42→38, CSI 0.376→0.362,
но **T_MAE 2.01→1.76** (−0.25 K).

Три CFOG случая паднаха (LBGO 2024-03-03, 2024-11-16, LBSF 2024-10-21).
TERM_DEBUG показа: мъглата **не умира от слънцето — тя никога не се
образува.** Поправката отне 1.5–2 h тъмно на гранични нощи с недостатъчно
нощно охлаждане. Fable: „не са дълг за връщане — старите HIT бяха отчасти
артефакт на грешното слънце."

**Остатъчна грешка:** ~10 min (рефракция 5 min + деклинация по Cooper до
10 min). Отложено, отделна тема.

---

## 3. ИЗГРЕВНАТА ПОПРАВКА (втората половина на сесията)

**Измерен дефект:** `T_skin` расте 5–6 K/h при изгрев, `T_air` стои
(0.03 K/h). Причини: `DZ_EFF` скача 8→500 m в един момент, и
`C_H_BULK=1.2e-3` е 9× малък.

**Решение (в `fog_model.py`):**
- `C_H_DAY = 0.01112` = κ²/(ln(z/z0m)·ln(z/z0h)) при z=0.5, z0m=0.03,
  z0h=0.003. Числото излиза от грапавостта, не е калибрирано.
- Рампа: активна само при `T_skin > T_air` И `sin_el > 0.02`, плавна до
  `sin_el = 0.15`. Нощният път байт-в-байт непокътнат.
- `DZ_EFF` расте log-линейно от 10 m (изгрев) до 300 m (`sin_el=0.5`).
- **Под по облачност:** `t = max(t, cf_now)` — плиткото изгревно `DZ` е
  физика на ЯСНА утрин; под плътен облак слоят е механично размесен още
  от нощта.

**Постигнато:** dT_air/dt в 06–07 UTC = 2.5–3.0 K/h (мишена 2.0–2.5).

**Важен урок:** първата версия без облачния под даде **11 MAE регресии,
всичките при CLDY**. Диагностиката показа скок +6 K/h в първия слънчев
час при `cf = 0.9–1.0`. Опус първо предположи „ICON е виновен" без
измерване; Fable спря запечатването и настоя за SEB_DEBUG. Механизмът се
оказа `DZ`, не `C_H` — и се поправи с един ред.

**Оставаща регресия:** `LBSF_CFOG_2024-03-02`, HIT→MISS. Диагностицирана:
мъглата се образува в 05h (един час преди изгрева), новото загряване я
вдига от земята в 06h, продължителността пада под критерия. Коренът е
нощният дефицит — `Tair` пада само 0.41 K/h за 11 часа.
**Илиан: „това не е MISS, а NEAR — дава сигнал."** `minVIS = 734 m`.

---

## 4. ЗАМРАЗЕНО — НОЩНИЯТ ПРОБЛЕМ

**Решение на Илиан, финално:** разделянето на тихите ясни нощи (мъгла
срещу фалшива аларма) е изчерпано с наличните данни. Единствената посока
е **по-добра инициализация**:
- вътре в РВД — данни от Vaisala вместо груб METAR
- извън РВД — `RHA` от SYNOP 18 UTC като флаг за разделяне, НЕ като
  инициализация

Мотив: моделът не може да моделира надеждно влагата 18–22, нито ICON
профилът е добра прогноза. Разделителят са **началните условия**.

**Как беше изчерпано (три независими начина):**

1. **Физика** — три етапа отхвърлени:
   - Етап 2 (C1+C2+DZ), Етап 2′ (C2 сам), Етап 3 (C2+C_H+rh_crit)
   - Обща причина: нощната мъгла виси на компенсиран баланс при
     `RH_CRIT=0.995`; 17 от 17 случая опират в тавана и стоят там
   - Етап 3 стендът: CFOG 12→9, ΔT знакът се обърна (+0.19 вместо −1.10)
     защото усиленият обмен пренася топлината на топлата почва нагоре

2. **Единични признаци** — `rh_early_max` дава +23.4% при кръстосана
   проверка за FA срещу CN, но **HIT и FA имат еднаква влажност**
   (0.992 срещу 0.981). Правилото убива мъгли колкото спасява FA:
   най-добър праг ΔCSI = +0.004 = шум.

3. **Двойки признаци** — Fisher-ова посока, 120 двойки, кръстосана
   проверка: най-добрата дава **+4.4%**. При разбъркани етикети −20%,
   тоест методът работи, сигналът го няма.

**Изводът:** информацията, която дели истинската мъгла от фалшивата, не е
в наличните величини (ICON профил, METAR, почва, облачност, вятър).

**GNSS и аерозол** — оставени за по-нататък. Измерено: формулата на
Kunkel за видимост не зависи от броя капки изобщо (морски→континентален =
фактор 1.8 във VIS — реален пропуск); GNSS дава само интегрирана колона,
не профил.

---

## 5. ФЛАГ ЗА НАДЕЖДНОСТ

`reliability.py` + `calibrate_reliability.py` + `reliability.json`.
Числата НЕ са зашити — преизчисляват се от `features.csv`.

**Валидирано:** прогноза „ЯСНО" при `rh_early_max > 0.95` на континентална
станция е вярна само в **42%** (Фишер p=0.0010). Тоест шест от десет
такива нощи са пропуснати мъгли.

Прогноза МЪГЛА континентална: **49–50%** — монета. Разслояването ѝ **не е
значимо** (p=0.077) и съзнателно не се съобщава.

LBGO има собствено значимо разслояване (p=0.0256).
LBBG: **нула мъглени случая** — предупреждение по станция в изхода.

Йерархия: базова честота от станцията, условен ефект от групата.

**Замразен** — не се развива до по-добра инициализация.

---

## 6. СЛЕДВАЩИ СТЪПКИ (по приоритет на Fable)

1. **Преизчисляване на `features.csv` и `reliability.json`** след
   изгревната поправка (незавършено от тази сесия).

2. **Морски летни мъгли (LBWN)** — механизмът е **неизвестен**, изобщо не
   е сондиран. Единственият клас грешки, при който не знаем защо моделът
   греши. LBWN 2026-07-21: моделът е 1.8 K по-**топъл** от наблюдението и
   въпреки това прави мъгла; премахва ~1.5 g/kg по-малко влага.
   *Най-голямо пространство за откритие.*

3. **LBBG набор** — нула мъглени случая. Станцията е неоценена по
   основната си задача. OGIMET, зими назад.

4. **LBSF лятна топлина (+2.40 K)** — заслужава **преизмерване**, защото
   част от нея може вече да е изядена от слънчевата и изгревната поправка.

5. **Категория NEAR в отчета** — предложение на Илиан. Пропуск с
   `minVIS < 2000 m` да се отчита отделно от слепите. В пробега преди
   изгревната поправка NEAR случаи няма нито един: от 29 пропуска 26 са с
   `minVIS ≥ 9000`. Промяна само в докладването, нула физика.

6. **Отложени слънчеви поправки** — рефракция (5 min) + деклинация по
   Cooper (до 10 min), заедно, с едно измерване.

7. **Отложени за преди калибрация на изгрева:** соларна константа
   `1361·0.75` при ниско слънце (реално пропускане 0.4–0.5); албедо при
   сняг (LBSF, LBGO зимата — фактор 3–4 в погълнатото SW).

---

## 7. РАБОТЕН ПРОТОКОЛ (спазван, работи)

- **Записана числова прогноза преди всеки пуск.** Пликът не се
  предоговаря след числата.
- **Диагностичен случай преди пълен пробег.** Единичният случай доказва
  „може", а не „ще" — Етап 2′ мина диагностиката и падна на пълния гейт.
- **Един ефект = едно измерване.** Компенсиращите двойки влизат заедно в
  един diff, но всяка има своя мишена.
- **Всеки консуматор на нова стойност е част от diff-а** (урок от v1.4).
- **Не се запечатва дефект с неизмерен механизъм**, когато диагностиката
  струва пет минути.
- **Триролев процес:** Илиан решава, Опус/Сонет изпълнява, Fable ревюира.

**Урок от тази сесия, най-скъпият:** „оперативен опит хваща
правдоподобно-грешните дефекти, които тестове не хващат." Гейтът мери
разлики, не истини. Слънчевата грешка от 110 минути беше невидима за
всяка автоматична проверка — хвана я синоптик, който знае кога изгрява
слънцето.

**Втори урок:** лутане с измерена всяка стъпка е сходимост, не хаос.
Изгревната поправка мина три итерации (само DZ → 0.42 K/h; DZ + Beljaars
w* → 0.48; DZ + C_HN от bulk теорията → в мишената). Всяка стъпка остави
число. Прибързаност щеше да е пълен пробег след първата.

---

## 8. ФАЙЛОВЕ

**Активни:** `fog_model.py`, `run_case.py`, `verify_cases.py`,
`pairing.py`, `reliability.py`, `calibrate_reliability.py`,
`reliability.json`, `collect_features.py`, `features.csv`

**Инструменти за анализ (нула пускания на модела):**
`analyse_features.py`, `validate_split.py`, `apply_rule.py`,
`combine_features.py`, `probe_mechanism.py` (ръчки u/kh/ch/g),
`campaign_seb.py`, `probe_discriminate.py`, `analyse_disc.py`,
`check_threshold.py`

**Репери:** `baselines\LBBG-v9`, `LBGO-v9`, `LBPD-v8`, `LBSF-v12`,
`LBWN-v11`

**Архив:** `fog_model_C2.py`, `run_case_C2.py`, `ETAP2_POSTMORTEM.md`,
`состояние_на_модела.txt`

**Известни дефекти в инструментите:** `campaign_seb.py` дели на нула при
мъгла (`G/|Rnet| = 514286`); сондата дава огромни числа в колоната за
роса при случаи без охлаждане. Не влияят на медианите, но трябва праг.

---

## 9. ЗА НОВИЯ ЧАТ

Работен език: български. Работна директория `C:\fogmodel`, среда
Anaconda `fogmodel`. Всички вятърни стойности в knots.

**Процедура:** последната версия на файловете е при Илиан; той ги качва
за работа върху тях.

Първо действие: преизчисляване на `features.csv` и `reliability.json`,
после морските летни мъгли на Варна.
